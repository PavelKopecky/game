{
  "name": "game",
  "version": "1.0.0",
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "scripts": {
    "build": "tsc && cp -r src public && mv build public/build",
    "dev": "tsc -w",
    "clean": "rm -r public/ 2>/dev/null"
  }
}
